{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsFuguAngularFileHandling",
  "title": "Fugu Angular File Handling Options Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "extensions": {
      "type": "string",
      "description": "Comma-seprated list of file extensions the app should be registered for as an editing program.",
      "x-prompt": "Which file extension(s) would you like your app to register for? (comma-separated, e.g.: .docx, .doc)"
    },
    "manifestPath": {
      "type": "string",
      "description": "The path to the web application manifest, relative to the project source root."
    }
  },
  "required": []
}
